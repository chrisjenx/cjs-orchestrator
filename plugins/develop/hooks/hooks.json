{
  "//": "Safe, stack-agnostic hook config. /develop:init copies worktree-guard.sh into the target's .claude/hooks/ and MERGES this block into .claude/settings.json (it does not overwrite existing hooks). The generic command timeout is set via env, not a per-stack hook.",
  "env": {
    "BASH_DEFAULT_TIMEOUT_MS": "120000",
    "BASH_MAX_TIMEOUT_MS": "600000"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/worktree-guard.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
