{
  "name": "fixture-node-ts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^1",
    "eslint": "^9"
  }
}
